---
import type { info } from "node_modules/astro/dist/core/logger/core";

/*let getImg=;
let getInfo=;
let getBtn=;

const Card={
    img:link,
    info:info,
    btn:btn,
};

function Card(link,info,btn){
         this.link=getImg;
         this.info=getInfo;
         this.btn=getBtn;
}*/
---
